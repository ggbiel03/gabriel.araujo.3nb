fn main() {
    let mut numeros: Vec<i32> = vec![10, 20, 30]; 
    
    // "! = variável não pode ser nula ou pode receber none"

    numeros.push(40); // Adicionando um elemento
    numeros.push(50);

    println!("Vetor: {:?}", numeros);
}
